---
import { db, Author, Comment } from "astro:db";

const comments = await db.select().from(Comment);

if (Astro.request.method === 'POST') {
  db.insert(Comment).values([
    { authorId: 1, content: "some comment2" },
  ])

  Astro.response.headers.append('Some', 'some')
}

---

<h2>Comments</h2>

{
  comments.map(({ authorId, content }) => (
    <article>
      <p>Author: {authorId}</p>
      <p>{content}</p>
    </article>
  ))
}

<form method="POST">
  <button type="submit">sbm</button>
</form>

